This game doesn't need the world's most fancy map system, it just needs to *work correctly*.
Don't be afraid of hardcoding for now.

Necessary:
	Load the map data
	Display the map

Expected:
	Save the map data (simple map editor)
	Handle collision data (separate "layer")

Several layers:
	1: base layer
	2: detail layer
	3: overlay (displayed above entities)
	4: collision (binary)

-------------------------

Region:
	-Uint16 x, y, z
	-Uint16 ***data
	
	LoadData(fname)
	SaveData(fname)
	
	NewData(x, y, z)
	DeleteData()
	
	SetTile(x, y, z, v)
	GetTile(x, y, z)
end

MapUtility:
	-Region region (vector/pagination?)
	-Image tileset
	-string tilesetName
	
	LoadMap(fname)
	SaveMap(fname)
	
	NewMap(x, y, z, tileset)
	DeleteMap()
	
	LoadTileset(fname) //used by LoadMap as well, used to set & switch tilesets
	
	DrawTo(screen, camX, camY)
end
